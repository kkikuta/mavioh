/*** 進捗状況 ***/
生徒((済)db -> (済)beans -> (済)dao -> servlet -> model -> jsp -> css)

/*** メモ ***/
・https://qiita.com/artery/items/e3fae99ea9416125ffda のJavaコーディング規約に沿ってコードを書く
・https://www.colordic.org/ の原色大辞典からCSSを書く
・AtomでMavioプロジェクトを開いてコピペしながらコーディングする

/*** 履歴 ***/
2020/2/2(日)	ログイン機能が完成
	 2/3(月)	掲示板トップ画面作成中、JSTL導入
	 2/4(火)	javadocに対応、掲示板画面完成、スケジュールトップ画面作成中

/*** DB情報 ***/

/* ユーザー */
CREATE TABLE USERS (
  ID INT PRIMARY KEY AUTO_INCREMENT,
  NAME VARCHAR(32) NOT NULL,
  PASSWORD VARCHAR(32) NOT NULL
);

/* コメント */
CREATE TABLE COMMENTS (
  ID INT PRIMARY KEY AUTO_INCREMENT,
  TITLE VARCHAR(32) NOT NULL,
  BODY VARCHAR(255) NOT NULL,
  USER_ID INT NOT NULL,
  POST_TIME VARCHAR(32) NOT NULL
);

/* イベント */
CREATE TABLE EVENTS (
  ID INT PRIMARY KEY AUTO_INCREMENT,
  TITLE VARCHAR(32) NOT NULL,
  DETAIL VARCHAR(255) NOT NULL,
  DATE DATE NOT NULL
);

/* 生徒 */
CREATE TABLE STUDENTS (
  ID INT PRIMARY KEY AUTO_INCREMENT,
  NAME VARCHAR(32) NOT NULL,
  GRADE INT NOT NULL,
  GENDER INT NOT NULL,
  SCHOOL VARCHAR(32) NOT NULL,
  DEVIATION_VALUE DOUBLE NOT NULL
);
